global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "servicio-principal"
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:8000"]

  - job_name: "servicio-b"
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:8001"]
